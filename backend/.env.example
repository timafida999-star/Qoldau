DATABASE_URL=postgresql://qoldau:qoldau@db:5432/qoldau
# REQUIRED. Generate one with: python -c "import secrets; print(secrets.token_urlsafe(64))"
# The app refuses to start with a placeholder or short value.
JWT_SECRET_KEY=
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080
UPLOAD_DIR=uploads

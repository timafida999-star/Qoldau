# Postgres credentials for the qoldau-db service. Must match backend/.env DATABASE_URL.
POSTGRES_USER=qoldau
POSTGRES_PASSWORD=change-me-to-a-strong-password
POSTGRES_DB=qoldau

# Public subpath the app is served under (trailing slash required).
VITE_BASE_PATH=/qoldau/
